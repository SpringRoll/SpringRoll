{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "pretest": "npm run build",
    "test": "lerna run test",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
