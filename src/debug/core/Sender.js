/**
 *
 *
 * @export
 * @class Sender
 */
export default class Sender {
  /**
   * Creates an instance of Sender.
   * @memberof Sender
   */
  constructor() {}

  /**
   *
   *
   * @memberof Sender
   */
  send() {
    fetch('', { method: 'POST' });
  }
}
